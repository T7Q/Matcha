{
    "name": "matcha",
    "version": "1.0.0",
    "description": "dating web application",
    "scripts": {
        "init": "npm update && cd backend && npm update",
        "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
        "setup_db": "cd backend && npm run setup_db",
        "drop_db": "cd backend && npm run drop_db"
    },
    "keywords": [],
    "author": "",
    "license": "MIT",
    "devDependencies": {
        "concurrently": "^5.3.0"
    },
    "dependencies": {
        "dotenv": "^8.2.0"
    }
}
