{
    "name": "matcha",
    "version": "1.0.0",
    "description": "dating web application",
    "scripts": {
        "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
        "create": "cd backend && npm run init_db && npm run create_tables",
        "setup": "cd backend && npm run setup",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "MIT",
    "devDependencies": {
        "concurrently": "^5.3.0"
    },
    "dependencies": {
        "dotenv": "^8.2.0"
    }
}
